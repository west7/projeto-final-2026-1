# Optional ML stack — kept out of the API image so it builds/serves without sklearn.
# scikit-learn is pinned exactly so joblib can deserialize the trained model reliably.
scikit-learn==1.9.0
joblib>=1.5
mlflow>=3.14
