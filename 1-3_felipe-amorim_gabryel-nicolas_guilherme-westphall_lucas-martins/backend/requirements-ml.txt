# ML stack used by training/evaluation and installed by the current model-enabled API image.
# Historical-only local deployments can omit this file when MODEL_PATH is unset.
# scikit-learn is pinned exactly so joblib can deserialize the trained model reliably.
scikit-learn==1.9.0
joblib>=1.5
mlflow>=3.14
